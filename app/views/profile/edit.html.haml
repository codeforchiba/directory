%section#profile
  %h1
    プロフィールの編集

  - if @user.errors.size > 0
    .alert.alert-danger{role: "alert"}
      エラーがあります。

  = form_for @user, url: profile_path, method: :patch, html: { class: "form-horizontal" } do |f|
    .form-group{class: @user.errors[:name].size > 0 && "has-error"}
      = f.label :name, class: "control-label"
      .control-column
        = f.text_field :name, class: "form-control"

    .form-group{class: @user.errors[:email].size > 0 && "has-error"}
      = f.label :email, class: "control-label"
      .control-column
        = f.email_field :email, class: "form-control"

    .form-group{class: @user.errors[:about].size > 0 && "has-error"}
      = f.label :about, class: "control-label"
      .control-column
        = f.text_area :about, rows: 5, class: "form-control"

    .form-group
      .control-column
        = f.submit class: "btn btn-lg btn-primary"
        = link_to profile_path, class: "btn btn-lg btn-default" do
          キャンセル

%section#profile
  %h1
    パスワードの変更

  - if @user.errors.size > 0
    .alert.alert-danger{role: "alert"}
      エラーがあります。

  = form_for @user, url: update_password_profile_path, method: :patch, html: { class: "form-horizontal" } do |f|
    .form-group{class: @user.errors[:current_password].size > 0 && "has-error"}
      = f.label :current_password, class: "control-label"
      .control-column
        = f.password_field :current_password, class: "form-control"

    .form-group{class: @user.errors[:password].size > 0 && "has-error"}
      = f.label :password, class: "control-label"
      .control-column
        = f.password_field :password, class: "form-control"

    .form-group{class: @user.errors[:password_confirmation].size > 0 && "has-error"}
      = f.label :password_confirmation, class: "control-label"
      .control-column
        = f.password_field :password_confirmation, class: "form-control"

    .form-group
      .control-column
        = f.submit class: "btn btn-lg btn-primary"
        = link_to profile_path, class: "btn btn-lg btn-default" do
          キャンセル
